
const first = [1, 2, 3];
const second = [4, 5, 6];

// concat will combine the two arrays
const combined = first.concat(second);

// slice will divide the array into two pieces
// returns all the elements after the index of 2
const slice = combined.slice(2);