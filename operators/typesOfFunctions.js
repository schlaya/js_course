// Performing a task
function greet(namen, lastName) {
  console.log(`Hello ${namen} ${lastName}`);
}

// Calculating a value
function square(number) {
  return number * number;
}

console.log(square(2));